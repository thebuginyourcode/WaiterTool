<Window x:Class="WaiterTool.Windows.TableNumberDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Table Number" Width="480" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F2F2F2"
        Closed="Window_Closed">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="Black" CornerRadius="6" Width="280" Height="170"
                HorizontalAlignment="Center">
            <Grid>
                <Image x:Name="PreviewImage" Stretch="Uniform" />
                <TextBlock x:Name="CameraStatusText" Foreground="White" HorizontalAlignment="Center"
                           VerticalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="12"
                           FontSize="12" Visibility="Collapsed" />
            </Grid>
        </Border>

        <TextBlock x:Name="HeaderText" Grid.Row="1" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"
                   Margin="0,16,0,0" TextWrapping="Wrap" TextAlignment="Center" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <TextBlock Text="Table #" VerticalAlignment="Center" FontSize="18" Margin="0,0,8,0" />
            <TextBox x:Name="TableNumberTextBox" Width="120" FontSize="20" Padding="6"
                     PreviewTextInput="TableNumberTextBox_PreviewTextInput"
                     KeyDown="TableNumberTextBox_KeyDown" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
            <Button Content="Cancel" Width="100" Padding="0,8" Margin="0,0,12,0" Click="Cancel_Click" />
            <Button x:Name="ConfirmButton" Content="Confirm &amp; Capture" Width="170" Padding="0,8"
                    Click="Confirm_Click" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
